name: 'Build Cocos Mobile'
description: 'Build cocos creator repositories for web-mobile'
inputs:
  game_id:
    description: 'Game ID'
    required: true
  cocos_version:
    description: 'Cocos Creator version path'
    required: true
runs:
  using: 'composite'
  steps:
    - name: Build web-mobile
      shell: cmd
      run: |
        echo cd game\%INPUT_GAME_ID% >> build.bat
        echo git restore --source=HEAD --staged --worktree assets >> build.bat
        echo CALL %INPUT_COCOS_VERSION% --no-cache --clear-cache --project . --build "platform=web-mobile;debug=false;md5Cache=true" >> build.bat
        echo IF EXIST build\web-mobile\5g_2.*.gif IF NOT EXIST build\web-mobile\5g_2.gif MOVE /Y build\web-mobile\5g_2.*.gif build\web-mobile\5g_2.gif >> build.bat

        CALL build.bat

        if %ERRORLEVEL%==36 exit 0
        if %ERRORLEVEL% NEQ 0 exit /b %ERRORLEVEL%

        REM Remove 'remote' from projectBundles and bundleVers in settings.json
        node -e "const fs=require('fs'),g=require('path'),b=g.join('game','%INPUT_GAME_ID%','build');if(!fs.existsSync(b)){console.log('build dir not found: '+b);process.exit(0)}const dirs=fs.readdirSync(b).filter(d=>d.startsWith('web-mobile')).sort().reverse();if(!dirs.length){console.log('no web-mobile dir');process.exit(0)}const d=g.join(b,dirs[0],'src');if(!fs.existsSync(d)){console.log('src dir not found: '+d);process.exit(0)}const f=fs.readdirSync(d).find(n=>n.startsWith('settings.')&&n.endsWith('.json'));if(!f){console.log('settings.json not found');process.exit(0)}const p=g.join(d,f),s=JSON.parse(fs.readFileSync(p,'utf8'));if(s.assets){s.assets.projectBundles=(s.assets.projectBundles||[]).filter(x=>x!=='remote');delete(s.assets.bundleVers||{})['remote'];fs.writeFileSync(p,JSON.stringify(s));console.log('Removed remote from settings.json: '+p)}"
      env:
        INPUT_GAME_ID: ${{ inputs.game_id }}
        INPUT_COCOS_VERSION: ${{ inputs.cocos_version }}
